/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */

import { buildMockModuleProps } from '@msdyn365-commerce/core-internal';
/// <reference types="jest" />

import * as React from 'react';
import * as renderer from 'react-test-renderer';

import TileNavigation from '../tile-navigation';
import { ITileNavigationData } from '../tile-navigation.data';
import { ITileNavigationConfig, ITileNavigationProps } from '../tile-navigation.props.autogenerated';

// const mockData: ITileNavigationData = {
//     categories: new AsyncResult<CategoryHierarchy[]>();
// };

const mockConfig: ITileNavigationConfig = {
    categoryId: 22565426195
};

const mockActions = {};

describe('TileNavigation', () => {
    let moduleProps: ITileNavigationProps<ITileNavigationData>;
    beforeAll(() => {
        moduleProps = buildMockModuleProps(mockActions, mockConfig) as ITileNavigationProps<ITileNavigationData>;
    });
    it('renders correctly', () => {
        const component: renderer.ReactTestRenderer = renderer.create(<TileNavigation {...moduleProps} />);
        const tree: renderer.ReactTestRendererJSON | renderer.ReactTestRendererJSON[] | null = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
});
