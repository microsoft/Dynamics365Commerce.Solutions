/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */

import { buildMockModuleProps } from '@msdyn365-commerce/core-internal';
/// <reference types="jest" />

import * as React from 'react';
import * as renderer from 'react-test-renderer';

import ApiTest from '../api-test';
import { IApiTestData } from '../api-test.data';
import { IApiTestConfig, IApiTestProps } from '../api-test.props.autogenerated';

/* eslint-disable  @typescript-eslint/no-explicit-any */
const mockcategoriesData: any = [];

const mockData: IApiTestData = {
    actionResponse: {
        text: 'Sample Response Data'
    },
    categories: mockcategoriesData
};

const mockConfig: IApiTestConfig = {
    showText: 'api-test'
};

const mockActions = {};

describe('ApiTest', () => {
    let moduleProps: IApiTestProps<IApiTestData>;
    beforeAll(() => {
        moduleProps = buildMockModuleProps(mockData, mockActions, mockConfig) as IApiTestProps<IApiTestData>;
    });
    it('renders correctly', () => {
        const component: renderer.ReactTestRenderer = renderer.create(<ApiTest {...moduleProps} />);
        const tree: renderer.ReactTestRendererJSON | renderer.ReactTestRendererJSON[] | null = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
});
